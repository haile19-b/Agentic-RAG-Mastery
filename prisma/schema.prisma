generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Data {
  id        Int    @id @default(autoincrement())
  content   String
  embedding Float[] 
}